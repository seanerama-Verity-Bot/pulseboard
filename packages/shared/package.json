{
  "name": "@pulseboard/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Contract types and pure constants shared by apps/server and apps/web (http-api-v1).",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
